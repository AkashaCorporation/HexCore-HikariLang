// HQL Bridge - placeholder module
// Will be expanded when integrating with hexcore-hql

pub struct HQLBridge;

impl Default for HQLBridge {
    fn default() -> Self {
        Self::new()
    }
}

impl HQLBridge {
    pub fn new() -> Self {
        HQLBridge
    }
}
